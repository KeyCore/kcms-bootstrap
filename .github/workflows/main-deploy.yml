name: main-deploy

on:
  release:
    types: [released]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo ${{ github.event.release.upload_url }}
        shell: bash
